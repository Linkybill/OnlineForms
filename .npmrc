# Proxy-Einstellungen
proxy=http://10.97.90.198:8080
https-proxy=http://10.97.90.198:8080

# Registry explizit setzen (optional, meist Default)
registry=https://registry.npmjs.org/

# Cache-Verzeichnis lokal im Agent-Workspace halten (vermeidet Locks im Userprofile)
cache=${HOME}/.npm-cache

# Strikter SSL (falls dein Proxy ein Zertifikat einsetzt → ggf. auf false setzen)
strict-ssl=true

# Optional: keine Fortschrittsbalken (macht Logs lesbarer in Pipelines)
progress=false

# Optional: immer UTF-8 Output
unicode=true